# Перетворення даних. Глобальні класи та функції.

## Зміст
1. Перетворення даних
1. Глобальні класи та функції

#
## Перетворення даних

Часто виникає потреба перетворити одні дані на інші. Деякі перетворення JavaScript виконує автоматично. Наприклад:

```javascript
let number1 = "56";
let number2 = 4;
let result = number1 + number2;
console.log(result); // 564
```

Тут змінна number1 представляє рядок, а точніше рядкове уявлення числа. А змінна number2 є числом. І в результаті ми отримаємо не число 60, а рядок 564.

При додаванні перетворення на JavaScript виробляються за принципом:

Якщо обидва операнда представляють числа, відбувається звичайне арифметичне додавання

Якщо попередня умова не виконується, то обидва операнди перетворюються на рядки і проводиться об'єднання рядків.

Відповідно, у прикладі вище, оскільки перший операнд - рядок, то другий операнд - число також перетворюється на рядок, і в результаті отримуємо рядок "564", а не число 60. Фактично ми отримуємо:
```javascript
let number1 = "56";
let number2 = 4;
let result = number1 + String(number2);
console.log(result); // 564
```
Функція String() дозволяє отримати рядкове подання значення. Так, вираз String(number2) отримує рядкову виставу number2, тобто з числа 4 отримує рядок "4".

Але стандартна поведінка не завжди може бути бажаною. Наприклад, у прикладі вище хочемо, щоб значення складалися не як рядки, бо як числа. У цьому випадку ми можемо використовувати функції перетворення.

### `parseInt()` та `parseFloat()`

Для перетворення рядка на ціле число застосовується функція `parseInt()`. Для перетворення рядків на дробові числа застосовується функція `parseFloat()`, яка працює аналогічним чином.

```javascript
const numStr = "56";
const num = 4;
const numStrWithText = "123Text";
const floatStr = "39.547";

console.log(parseInt(numStr)); // 57
console.log(parseInt(num)); // 4
console.log(parseInt(numStrWithText)); // 123
console.log(parseInt(floatStr)); // 39

console.log(parseFloat(floatStr)); // 39.547

const binary1 = "01010";
const binary2 = "01101";
const hex1 = "f1";
const hex2 = "ca3";

console.log(parseInt(binary1, 2));
console.log(parseInt(binary2, 2));
console.log(parseInt(hex1, 16));
console.log(parseInt(hex2, 16));
```

### `NaN` та `isNaN()`

Якщо функції `parseInt()` не вдасться виконати перетворення, то він повертає значення `NaN (Not a Number)`, яке говорить про те, що рядок не є числом і не може бути перетворений.

```javascript
console.log(parseInt("abc"));   // NaN
let type = typeof NaN;
console.log(type);              // number
```
Що цікаво, саме значення `NaN` (не число) є типом числа.

За допомогою спеціальної функції `isNaN()` можна перевірити, чи рядок представляє число. Якщо рядок не є числом, то функція повертає true, якщо це число - `false`:

```javascript
let num1 = "javascript";
let num2 = "22";
let result = isNaN(num1);
console.log(result); // true
     
result = isNaN(num2);
console.log(result); //  false
```


#
## Глобальні класи та функції

### `String`

Кожна змінна з типом `String` є представником цього класу.
Використавши модифікатор доступу `.` (крапка), модна отримати доступ до параметрів і методів.

| Метод (функція) / Параметр (змінна) | Опис |
|-------------------------------------|------|
|`length`               | довжина рядка |
|`at(n)`                | повертає символ рядка за індексом, останній за індексом `-1`, де `n` - індекс |
|`indexOf(str)`         | повертає індекс символу у рядку, де `str` - символ |
|`toLocaleLowerCase()`  | змінює регістр рядку на нижній |
|`toLocaleUpperCase()`  | змінює регістр рядку на верхній |
|`includes(str)`        | перевіряє чи є значення у рядку, де `str` - значення |
|`trim()`               | прибирає "пусті" символи з початку і кінця рядка |
|`trimStart()`          | прибирає "пусті" символи з початку рядка |
|`trimEnd()`            | прибирає "пусті" символи з кінця рядка |
|`concat(str)`          | з'єднує два рядки, де `str` - другий рядок |
|`replace(str, replStr)`| заміняє значення `str` на `replStr` у рядку |
|`slice(i,j)`           | обрізає рядок з індекса `i` до індекса `j` |
|`split(str)`           | роз'єднує рядок по символу і повертає масив значень, де `str` - символ  |


### `Number`

Кожна змінна з типом `Number` є представником цього класу.
Використавши модифікатор доступу `.` (крапка), модна отримати доступ до методів.

| Метод (функція) / Параметр (змінна) | Опис |
|-------------------------------------|------|
|`toFixed(n)`       | округляє дробове число, де `n` - кількість цифр після точки |
|`toExponential()`  | перетворює число на експоненціальне |
|`toString()`       | перетворює число на рядковий тип |


### `Symbol`

Кожна змінна з типом `Symbol` є представником цього класу.
Використавши модифікатор доступу `.` (крапка), модна отримати доступ до параметрів.

| Метод (функція) / Параметр (змінна) | Опис |
|-------------------------------------|------|
|`Symbol(str)`  | створює унікальну змінну з типом `Symbol`, де `str` - це `description` символа |
|`description`  | значення передане у дижки під час створення змінної |


### `Array`
Масиви - це змінні з типом `Object`, але вони мають свої власні методи та параметри.
Використавши модифікатор доступу `.` (крапка), модна отримати доступ до параметрів.

| Метод (функція) / Параметр (змінна) | Опис |
|-------------------------------------|------|
|`Array.isArray(arr)`         | перевіряє чи є змінна масивом, де `arr` - масив |
|`length`                     | довжина масива |
|`at(n)`                      | повертає значення масива за індексом, останній за індексом `-1`, де `n` - індекс |
|`indexOf(val)`               | повертає індекс значення у масиві, де `val` - значення |
|`concat(arr)`                | з'єднує два масиви, де `arr` - другий масив |
|`includes(val)`              | перевіряє чи є значення у масиві, де `val` - значення |
|`toString()`                 | перетворює масив на рядок, розділяючи кожен елемент комою |
|`join(str)`                  | перетворює масив на рядок, розділяючи кожен елемент рядковим значенням, де `str` - рядкове значення |
|`reverse()`                  | "перевертає" масив задом-наперед |
|`sort(<optional callback>)`  | сортує масив, `optional callback` - необов'язкова функція-правило для сортування |
|`push(val)`                  | додає елемент до кінця масиву, де `val` - новий елемент |
|`pop()`                      | видаляє та повертає останній елемент масиву |
|`forEach((item, i) => {});`  | цикл для перебору значень масиву, де `item` - значення, `i` - індекс |
|`map((item) => {});`         | цикл котрий переберає і повертає `кожне` значення до нового масиву, де `item` - значення |
|`filter((item) => {});`      | цикл котрий переберає і повертає `вибране` значення до нового масиву, де `item` - значення|


### `Date`

Клас `Date` використовюється для роботи з датами та часом.
Використавши модифікатор доступу `.` (крапка), модна отримати доступ до параметрів.

| Клас / Метод (функція) | Опис |
|-------------------------------------|------|
|`Date.now()`           | повертає кількість секунд з 1 січня 1970 року |
|`new Date()`           | створює дату |
|`getDate()`            | повертає число календарного місяця |
|`getDay()`             | повертає інднекс дня тижня |
|`getFullYear()`        | повернтає рік дати  |
|`getHours()`           | повертає годину дати |
|`getMilliseconds()`    | повертає значення мілісекунд дати |
|`getMinutes()`         | повертає хвилини дати |
|`getMonth()`           | повертає індекс місяця дати |
|`getSeconds()`         | повертає секунди дати |
|`getUTCDate()`         | повертає число календарного місяця по Грінвічу |
|`getUTCDay()`          | повертає інднекс дня тижня по Грінвічу |
|`getUTCFullYear()`     | повернтає рік дати по Грінвічу |
|`getUTCHours()`        | повертає годину дати по Грінвічу |
|`getUTCMilliseconds()` | повертає значення мілісекунд дати по Грінвічу |
|`getUTCMinutes()`      | повертає хвилини дати по Грінвічу |
|`getUTCMonth()`        | повертає індекс місяця дати по Грінвічу |
|`getUTCSeconds()`      | повертає секунди дати по Грінвічу |
|`getTime()`            | повертає кількість секунд з 1 січня 1970 року |
|`toDateString()`       | повертає рядок з форматованою датою |
|`toISOString()`        | повертає дату і час у рядковому ISO форматі|
|`toLocaleDateString()` | повертає дату у рядковому локальному форматі |
|`toLocaleString()`     | повертає час і дату у рядковому локальному форматі |
|`toLocaleTimeString()` | повертає час у рядковому локальному форматі |
|`toTimeString()`       | повертає час у рядковому форматі |


### `Math`

Клас `Math` міститиь у собі функціонал для роботи з алгеброю.
Використавши модифікатор доступу `.` (крапка), модна отримати доступ до параметрів.

| Метод (функція) / Параметр (змінна) | Опис |
|-------------------------------------|------|
|`Math.PI`         | значення числа `π` |
|`Math.abs(n)`     | перетворює число на абослютне(додатнє), де `n` - число |
|`Math.floor(n)`   | округляє дробове число до "нижнього" значення цілого, де `n` - число |
|`Math.round(n)`   | округляє дробове число до цілого, де `n` - число |
|`Math.random()`   | повертає випадкове число від 0 до 1 |
|`Math.max(...n)`  | повертає найбільше з чисел, де `...n` - числа |
|`Math.min(...n)`  | повертає найменше з чисел, де `...n` - числа |
|`Math.pow(n, deg)`| зводе число до ступеня , де `n` - число та `deg` - ступінь |
|`Math.sqrt(n)`    | повертає корінь квадратний числа, де `n` - число |


### `Timers`

| Функція | Опис |
|---------|------|
|`setTimeout(<callback func>, <time>)`  | створює таймаут з виконання функції, де `callback func` - функція для виконання та `time` - час до виконання |
|`setInterval(<callback func>, <time>)` | створює інтервал з виконанням функції, де `callback func` - функція для виконання та `time` - інтервал виконання |

### `Object`
